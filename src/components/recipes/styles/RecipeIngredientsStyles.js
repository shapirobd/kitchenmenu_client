import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
	root: {
		paddingTop: 0,
		paddingBottom: 0,
	},
	main: {
		margin: "0 0 20px 0",
		backgroundColor: "#fff",
		height: "100%",
		// padding: "20px 0 0 0"
	},
}));
